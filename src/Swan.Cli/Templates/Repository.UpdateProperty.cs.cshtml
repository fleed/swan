@using Swan.Generators.Models
@model PropertyViewModel

            if (existing.@(this.Model.Name) != converted.@(this.Model.Name))
            {
                changed = true;
                existing.@(this.Model.Name) = converted.@(this.Model.Name);
            }
            